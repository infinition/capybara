/target

# La cle de dechiffrement et les chemins personnels ne doivent jamais entrer
# dans l'historique. Un git add -A suffit a les y remettre, et une reecriture
# d'historique coute une demi journee.
/cle-device.txt

# Les dumps et les instantanes n'ont rien a faire ici non plus.
*.bin
*.tamastate
*.tamasave

# Fichiers binaires externes et en-tetes tiers.
*.pack

# Les notes de travail restent locales. Feuille de route, passation, releves
# materiels, dossier de retro-ingenierie : c'est le carnet de l'enquete, pas de
# la documentation publiee. La page publique est index.html, a la racine.
/docs/
